import { GlobalSettings } from "@/features/properties/GlobalSettings";

export default function SystemSettingsPage() {
  return (
    <main className="mx-auto w-full max-w-5xl px-4 py-8 md:px-8">
      <GlobalSettings />
    </main>
  );
}
